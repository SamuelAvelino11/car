spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/Concecionaria?useTimezone=true&serverTimezone=UTC
    username: root
    password: admin123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibertane:
        dialect: org.org.hibernate.dialect.MySQLDialect
    show-sql: false
Apis:
  security:
    token:
      secrets: ${JWT_SECRET:my-secret-key}